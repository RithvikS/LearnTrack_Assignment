package com.learntrack.entity;

/**
 * Enum for enrollment status - BONUS feature.
 * Ensures only valid statuses are used.
 */
public enum EnrollmentStatus {
    ACTIVE,
    COMPLETED,
    CANCELLED
}
